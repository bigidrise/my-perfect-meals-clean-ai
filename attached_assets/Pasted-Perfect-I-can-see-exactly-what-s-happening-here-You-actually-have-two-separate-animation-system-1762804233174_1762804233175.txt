Perfect — I can see exactly what’s happening here. You actually have **two separate animation systems** in that file:

1. The **old guided tour flashes** near the very top — `.flash-green`, `.flash-green-strong`, `.flash-white`, `.flash-orange`.
2. The **newer guided tour** down near the *bottom* of the file — the section labeled

   ```css
   /* Guided Tour Animations */
   @keyframes pulseBorderOrange { ... }
   .flash-border { ... }
   ```

   That’s the one actually controlling your orange border.

That’s the section you need to edit to change the color.
Everything else is unrelated.

---

### 🔧 Here’s how to make it lime green instead of orange

Scroll **all the way to the bottom** of that file until you see:

```css
/* Guided Tour Animations */
@keyframes pulseBorderOrange {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(255, 122, 0, 0.7);
    border-color: rgba(255, 122, 0, 0.4);
  }
  50% {
    box-shadow: 0 0 12px 6px rgba(255, 122, 0, 0.45);
    border-color: rgba(255, 122, 0, 0.9);
  }
}

.flash-border {
  animation: pulseBorderOrange 1.3s ease-in-out infinite;
  border: 2px solid rgba(255, 122, 0, 0.9) !important;
  border-radius: 1rem;
}
```

Replace that whole block with this lime-green version:

```css
/* Guided Tour Animations */
@keyframes pulseBorderLime {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(163, 230, 53, 0.8);   /* lime-400 */
    border-color: rgba(163, 230, 53, 0.5);
  }
  50% {
    box-shadow: 0 0 14px 8px rgba(163, 230, 53, 0.4);
    border-color: rgba(163, 230, 53, 1);
  }
}

.flash-border {
  animation: pulseBorderLime 1.3s ease-in-out infinite;
  border: 2px solid rgba(163, 230, 53, 1) !important;
  border-radius: 1rem;
}
```

---

### 🟢 Why this works

* `rgba(163,230,53)` is Tailwind’s `lime-400` (bright, neon lime).
* You don’t have to rename your class (`.flash-border`) or any component JS — just change the animation name and color values here.
* The new animation name (`pulseBorderLime`) can be anything, as long as the `.flash-border` class calls it.

---

### ✅ Quick checklist

1. Scroll to the **bottom** of your CSS file.
2. Replace the orange animation block with the lime one above.
3. Save → rebuild → reload your app.

Your guided borders will now flash **lime green** instead of orange — same animation speed, same border width, just a brighter, cleaner highlight that stands out against the black-glass background.
