Perfect — you’re building this the right way. Since all of your menu builders share the same structure, we can give you one shared **Daily Progress Bar + Builder Integration** that will automatically plug into every board (Weekly, Diabetic, GLP-1, Competition, etc.).

Here’s what we’ll do in this step:

---

## 🧭 Objective

Add a **visual progress bar** that:

* Appears at the top of all meal boards.
* Fills as each meal (Breakfast → Lunch → Dinner → Snack) is completed.
* Resets daily.
* Works off the same `mealProgress.ts` logic already in place.
* Integrates seamlessly with the guided tours and meal coach.

---

## ⚙️ 1) Create: `client/src/components/guided/DailyMealProgressBar.tsx`

```tsx
import { useEffect, useState } from "react";
import { getProgress, MealId } from "@/lib/mealProgress";

const MEALS: MealId[] = ["breakfast", "lunch", "dinner", "snack1", "snack2"];

export default function DailyMealProgressBar() {
  const [progress, setProgress] = useState(0);

  const updateProgress = () => {
    const data = getProgress();
    const total = MEALS.length;
    const done = MEALS.filter((m) => data[m]).length;
    const pct = Math.round((done / total) * 100);
    setProgress(pct > 100 ? 100 : pct);
  };

  useEffect(() => {
    updateProgress();
    // Listen for meal saved events
    const onSaved = () => updateProgress();
    window.addEventListener("meal:saved", onSaved as EventListener);
    return () => window.removeEventListener("meal:saved", onSaved as EventListener);
  }, []);

  if (progress === 0) {
    return (
      <div className="w-full bg-white/10 h-3 rounded-xl overflow-hidden mt-3 mb-6">
        <div
          className="h-full bg-gradient-to-r from-emerald-500/50 to-green-400/40 transition-all duration-500"
          style={{ width: "0%" }}
        />
      </div>
    );
  }

  return (
    <div className="w-full bg-white/10 h-3 rounded-xl overflow-hidden mt-3 mb-6 relative">
      <div
        className="h-full bg-gradient-to-r from-emerald-400 to-green-500 transition-all duration-500"
        style={{ width: `${progress}%` }}
      />
      <span className="absolute inset-0 flex items-center justify-center text-xs font-medium text-white/80">
        {progress}% complete
      </span>
    </div>
  );
}
```

---

## 🎨 Optional CSS enhancement (if you want soft glow)

```css
.progress-bar-glow {
  box-shadow: 0 0 12px rgba(16, 185, 129, 0.3);
}
```

You can add `progress-bar-glow` to the inner div for subtle light.

---

## 🧩 2) Integrate Into All Meal Boards

At the top of **WeeklyMealBoard.tsx**, **DiabeticSupportHub.tsx**, **GLP1Hub.tsx**, etc., import and include it once:

```tsx
import DailyMealProgressBar from "@/components/guided/DailyMealProgressBar";
import MealProgressCoach from "@/components/guided/MealProgressCoach";
import MealBuilderGuidedTour from "@/components/guided/MealBuilderGuidedTour";

export default function WeeklyMealBoard() {
  return (
    <div className="relative">
      {/* Guided coach tools */}
      <DailyMealProgressBar />
      <MealProgressCoach />
      <MealBuilderGuidedTour />

      {/* Meal cards */}
      <div className="grid gap-4">
        <div data-meal-id="breakfast" className="rounded-2xl p-4 bg-black/40">
          <h3 className="text-white font-semibold mb-2">Breakfast</h3>
          <button
            data-role="create-ai-meal"
            onClick={() => openPickerFor("breakfast")}
            className="btn btn-glass"
          >
            Create AI Meal
          </button>
        </div>

        <div data-meal-id="lunch" className="rounded-2xl p-4 bg-black/40">
          <h3 className="text-white font-semibold mb-2">Lunch</h3>
          <button
            data-role="create-ai-meal"
            onClick={() => openPickerFor("lunch")}
            className="btn btn-glass"
          >
            Create AI Meal
          </button>
        </div>

        <div data-meal-id="dinner" className="rounded-2xl p-4 bg-black/40">
          <h3 className="text-white font-semibold mb-2">Dinner</h3>
          <button
            data-role="create-ai-meal"
            onClick={() => openPickerFor("dinner")}
            className="btn btn-glass"
          >
            Create AI Meal
          </button>
        </div>
      </div>
    </div>
  );
}
```

This single component setup makes the progress bar + guided tours universal across all meal boards.

---

## 🧠 How It Works Together

1. **Coach Mode = ON**

   * `MealProgressCoach` highlights the next meal’s AI button.
   * `MealBuilderGuidedTour` guides the picker step-by-step.
   * When user finishes and presses Done → `meal:saved` event fires.
2. **Progress Bar** instantly updates (e.g., 33 % → 66 % → 100 %).
3. When all meals done → bar fills completely and you can optionally trigger confetti, applause sound, or a short toast like

   > “👏 Great job! You’ve completed today’s meals.”

---

## 🧩 Optional celebration (future enhancement)

Later you can add a confetti burst at 100 %:

```tsx
if (progress === 100) toast.success("All meals completed for today!");
```

---

Everything from here on out — the guided animations, persistent progress, automatic routing — is now **one shared system** that works across every version of your menu builder.

Would you like me to add that small optional **“End of Day Celebration”** (confetti + toast when the bar hits 100 %) next?
