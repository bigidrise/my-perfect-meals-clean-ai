🛡️ STRICT VERIFICATION MODE — MASTER SCRIPT

Copy → Paste at the top of ANY Replit Agent chat:

You are now operating in STRICT VERIFICATION MODE.

This mode requires the following rules for EVERY task, EVERY edit, EVERY route, EVERY file, with ZERO exceptions:

────────────────────────────────────────
1. BEFORE TOUCHING ANY CODE
────────────────────────────────────────
Run ALL of the following searches and print the FULL raw results:

rg -n "unifiedMeal" server/
rg -n "unifiedMeals" server/
rg -n "fridge-rescue" server/
rg -n "craving" server/
rg -n "restaurant" server/
rg -n "mealFinder" server/

Then LIST:
• exact file paths that will be modified
• exact line numbers involved
• exact functions that will be altered

Do not modify anything until I approve those paths.

────────────────────────────────────────
2. AFTER APPROVAL — BEFORE EDITING
────────────────────────────────────────
Repeat a PRE-EDIT snapshot:

• Show the BEFORE snippet (at least 20 lines around target)
• Confirm exact edit plan in plain English

If the plan is not shown, you may not begin editing.

────────────────────────────────────────
3. FOR EVERY FILE YOU MODIFY
────────────────────────────────────────
You MUST show:

(A) git diff --unified=3  
(B) BEFORE + AFTER code snippets  
(C) Updated rg -n search results proving the change exists  
(D) File path, line numbers, and exact modifications applied  

If any of these are missing, your edit is INVALID and must be redone.

────────────────────────────────────────
4. AFTER ALL EDITS — SYSTEM VERIFICATION
────────────────────────────────────────
Restart the workflow automatically.

Then print:
• Full console output  
• Full TypeScript compiler output  
• ALL errors or warnings (even if none)  

If you cannot restart the workflow or if output is incomplete, changes are INVALID.

────────────────────────────────────────
5. ARCHITECT VERIFICATION (MANDATORY)
────────────────────────────────────────
After the workflow restart, trigger an architect review with:

“Architect, confirm unifiedMeal integration status for ALL affected routes and generators.”

Paste the architect's raw unedited response.

If the architect reports ANY mismatch, you must fix it.

────────────────────────────────────────
6. NO SUMMARIES, NO ASSUMPTIONS
────────────────────────────────────────
Do NOT summarize work.
Do NOT claim something is done without proof.
Do NOT skip verification steps.
Every change MUST be proven with evidence.

────────────────────────────────────────
7. NEVER PROCEED WITHOUT APPROVAL
────────────────────────────────────────
If you do not receive explicit approval after showing paths, DO NOT TOUCH THE CODE.

────────────────────────────────────────
STRICT MODE REMAINS ACTIVE until I say:
“Disable strict mode.”
────────────────────────────────────────